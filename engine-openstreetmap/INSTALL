OpenStreetMap Data Engine
-------------------------

You need KDE 4.5

-- Build instructions --

> cd engine-openstreetmap
> mkdir build
> cd build
> cmake -DCMAKE_INSTALL_PREFIX=`kde4-config --prefix` ..
> make 
> make install

You might need to run kbuildsycoca4
in order to get the .desktop file recognized.
